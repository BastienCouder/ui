---
title: Sheet
description: A sheet is a transient view that appears above other content on the screen when a user interacts with a control.
thumbnail:
  image: /previews/components/core/avatar.jpeg
  video: /previews/components/core/avatar.mp4
---

<ComponentPreview
  name="demos/components/core/react/overlay/sheet/default"
  preview={`<Sheet content={<Content/>}>
   <Button variant="outline">Open</Button>
</Sheet>`}
/>

## Installation

Install the following dependencies:

```bash
npm install @radix-ui/react-dialog
```
Copy and paste the following code into your project.<br/>
<ComponentSource name={["components/core/default/react/overlay/sheet"]} extension={["tsx"]} />

## Options

### Side

Use the `side` prop to control the side from which the sheet will appear. By default, it opens from the right.

<ComponentPreview name="demos/components/core/react/overlay/sheet/side" 
preview={`<Sheet content={<Content />} side="left"> 
  <Button variant="outline">Open Left</Button> 
</Sheet>`} 
/>

### Composition

Here’s a full example combining all the components: SheetTrigger, SheetContent, SheetHeader, SheetFooter, SheetClose, and SheetOverlay.

<ComponentPreview name="demos/components/core/react/overlay/sheet/composition" 
preview={`<Sheet>
  <SheetTrigger>Open</SheetTrigger>
  <SheetContent>
    <SheetHeader>
      <SheetTitle>Are you absolutely sure?</SheetTitle>
      <SheetDescription>
        This action cannot be undone. This will permanently delete your account
        and remove your data from our servers.
      </SheetDescription>
    </SheetHeader>
  </SheetContent>
</Sheet>`} />