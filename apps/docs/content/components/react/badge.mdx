---
title: Badge
description: Displays a badge or a component that looks like a badge.
component: true
---

<ComponentPreview name="demos/ui/react/badge/default"
preview={`<Badge>Badge</Badge>`}
 />

## Installation

<Tabs defaultValue="cli">
<TabsList>
<TabsTrigger value="cli">
CLI
</TabsTrigger>
<TabsTrigger value="manual">  
Manual
</TabsTrigger>
</TabsList>

<TabsContent value="cli">
<Bash>
<BashCommand>npx</BashCommand> mbi add badge
</Bash>
</TabsContent>

<TabsContent value="manual">
<Steps>
  <Step>Copy and paste the following code into your project.</Step>
  <ComponentSource
    name={["ui/react/badge"]}
    extension={["tsx"]}
  />
  <Step>Update the import paths to match your project setup.</Step>
</Steps>
</TabsContent>
</Tabs>

## Options

### Variant

Use the `variant` prop to control the visual style of the `Badge`. The default variant is `primary`.

<ComponentPreview
  name="demos/ui/react/badge/variant"
  preview={`
  <Badge variant="neutral">Neutral</Badge>
  <Badge variant="outline">Outline</Badge>
  <Badge variant="primary">Primary</Badge>
  <Badge variant="success">Success</Badge>
  <Badge variant="danger">Danger</Badge>
  <Badge variant="warning">Warning</Badge>
  <Badge variant="accent">Accent</Badge>
`}
/>

### Size

Use the `size` prop to control the size of the `Badge`. The default variant is `sm`.

<ComponentPreview
  name="demos/ui/react/badge/size"
  preview={`<Badge variant="primary" size="sm">
  Small
</Badge>
<Badge variant="primary" size="md">
  Medium
</Badge>
<Badge variant="primary" size="lg">
  Large
</Badge>`}
/>

### Icon

To add additional context for the `Badge`, use the `icon` prop to add an icon.

<ComponentPreview
  name="demos/ui/react/badge/icon"
  preview={`<Badge icon={<ShieldIcon />} size="sm">
  Badge
</Badge>
<Badge icon={<ShieldIcon />} size="md">
  Badge
</Badge>
<Badge icon={<ShieldIcon />} size="lg">
  Badge
</Badge>`}
/>
