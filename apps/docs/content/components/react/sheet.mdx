---
title: Sheet
description: Extends the Dialog component to display content that complements the main content of the screen.
component: true
---

<ComponentPreview
  name="demos/ui/react/sheet/default"
  preview={`<Sheet content={<Content/>}>
   <Button variant="outline">Open</Button>
</Sheet>`}
/>

## Installation

<Tabs defaultValue="cli">
<TabsList>
<TabsTrigger value="cli">
CLI
</TabsTrigger>
<TabsTrigger value="manual">  
Manual
</TabsTrigger>
</TabsList>

<TabsContent value="cli">
<Bash>
<BashCommand>npx</BashCommand> revueui add sheet
</Bash>
</TabsContent>

<TabsContent value="manual">
<Steps>
<Step className='flex items-center gap-2 mb-2'>Install the following dependencies:<SelectPackageManager/></Step>

<Bash>
<PackageManager/> <p className='flex ml-2'>@radix-ui/react-dialog</p>
</Bash>

  <Step>Copy and paste the following code into your project.</Step>
  <ComponentSource name={["ui/react/sheet"]} extension={["tsx"]} />
  <Step>Update the import paths to match your project setup.</Step>
</Steps>
</TabsContent>
</Tabs>

## Options

### Side

Use the `side` prop to control the side from which the sheet will appear. By default, it opens from the right.

<ComponentPreview name="demos/ui/react/sheet/side" 
preview={`<Sheet content={<Content />} side="left"> 
  <Button variant="outline">Open Left</Button> 
</Sheet>`} 
/>

### Full Example

Here’s a full example combining all the components: SheetTrigger, SheetContent, SheetHeader, SheetFooter, SheetClose, and SheetOverlay.

<ComponentPreview name="demos/ui/react/sheet/composition" 
preview={`<Sheet>
  <SheetTrigger>Open</SheetTrigger>
  <SheetContent>
    <SheetHeader>
      <SheetTitle>Are you absolutely sure?</SheetTitle>
      <SheetDescription>
        This action cannot be undone. This will permanently delete your account
        and remove your data from our servers.
      </SheetDescription>
    </SheetHeader>
  </SheetContent>
</Sheet>`} />