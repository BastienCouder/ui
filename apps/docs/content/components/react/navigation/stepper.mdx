---
title: Stepper
description: A stepper is a user interface element that displays a sequence of steps in a linear order.
thumbnail:
  image: /previews/components/core/avatar.jpeg
  video: /previews/components/core/avatar.mp4
---

<ComponentPreview
  name="demos/components/core/react/navigation/stepper/default"
  preview={`<Stepper initialStep={0} steps={steps}>
    {steps.map((stepProps, index) => {
        return (
        <Step key={stepProps.label} {...stepProps}>
            <div className="bg-secondary text-primary my-2 flex h-40 items-center justify-center rounded-md border">
                <h1 className="text-xl">
                     Step
                    {index + 1}
                </h1>
            </div>
        </Step>
        )
    })}
    <Footer />
</Stepper>`}
/>

## Installation

Copy and paste the following code into your project.<br/>
  <ComponentSource name={["components/core/default/react/navigation/stepper"]} extension={["tsx"]} />

Update tailwind.config.js
  
  ```js 
  /** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      keyframes: {
        "collapsible-down": {
          from: { height: "0" },
          to: { height: "var(--radix-collapsible-content-height)" },
        },
        "collapsible-up": {
          from: { height: "var(--radix-collapsible-content-height)" },
          to: { height: "0" },
        },
      },
      animation: {
        "collapsible-down": "collapsible-down 0.2s ease-out",
        "collapsible-up": "collapsible-up 0.2s ease-out",
      },
    },
  },
}
  ```
 Update the import paths to match your project setup.

## Options

### Orientation

Use the props `orientation` prop to the Stepper component to set the orientation as "vertical" or "horizontal".

<ComponentPreview
  name="demos/components/core/react/navigation/stepper/orientation"
  preview={`<Stepper initialStep={0} steps={steps}>
    {steps.map((stepProps, index) => {
        return (
        <Step key={stepProps.label} {...stepProps}>
            <div className="bg-secondary text-primary my-2 flex h-40 items-center justify-center rounded-md border">
                <h1 className="text-xl">
                     Step
                    {index + 1}
                </h1>
            </div>
        </Step>
        )
    })}
    <Footer />
</Stepper>`}
/>

### Optional steps

If you want to make a step optional, you can add `optional: true` in the array of steps.
In this example, the second step is optional. You can visualize the change of the label in the `Next` button

<ComponentPreview
  name="demos/components/core/react/navigation/stepper/optional"
  preview={`<Stepper initialStep={0} steps={steps}>
    {steps.map((stepProps, index) => {
        return (
        <Step key={stepProps.label} {...stepProps}>
            <div className="bg-secondary text-primary my-2 flex h-40 items-center justify-center rounded-md border">
                <h1 className="text-xl">
                     Step
                    {index + 1}
                </h1>
            </div>
        </Step>
        )
    })}
    <Footer />
</Stepper>`}
/>

### Variants

Use the `variant` prop to customize the stepper style.

<ComponentPreview
  name="demos/components/core/react/navigation/stepper/variant"
  preview={`<Stepper initialStep={0} steps={steps}>
    {steps.map((stepProps, index) => {
        return (
        <Step key={stepProps.label} {...stepProps}>
            <div className="bg-secondary text-primary my-2 flex h-40 items-center justify-center rounded-md border">
                <h1 className="text-xl">
                     Step
                    {index + 1}
                </h1>
            </div>
        </Step>
        )
    })}
    <Footer />
</Stepper>`}
/>

### Size

Use the `size` prop to adjust the size of the stepper.

<ComponentPreview
  name="demos/components/core/react/navigation/stepper/size"
  preview={`<Stepper initialStep={0} steps={steps}>
    {steps.map((stepProps, index) => {
        return (
        <Step key={stepProps.label} {...stepProps}>
            <div className="bg-secondary text-primary my-2 flex h-40 items-center justify-center rounded-md border">
                <h1 className="text-xl">
                     Step
                    {index + 1}
                </h1>
            </div>
        </Step>
        )
    })}
    <Footer />
</Stepper>`}
/>
