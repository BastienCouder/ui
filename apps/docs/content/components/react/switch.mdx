---
title: Switch
description: A control that allows the user to toggle between checked and not checked.
component: true
---

<ComponentPreview
  name="demos/ui/react/switch/default"
  extension={"tsx"}
  preview={`<div className="flex items-center space-x-2">
    <Switch id="airplane-mode" />
    <Label htmlFor="airplane-mode">Airplane Mode</Label>
</div>`}
/>

## Installation

<Tabs defaultValue="cli">
<TabsList>
<TabsTrigger value="cli">
CLI
</TabsTrigger>
<TabsTrigger value="manual">  
Manual
</TabsTrigger>
</TabsList>

<TabsContent value="cli">
<Bash>
<BashCommand>npx</BashCommand> mbi add breadcrumb
</Bash>
</TabsContent>

<TabsContent value="manual">
<Steps>
<Step className='flex items-center gap-2 mb-2'>Install the following dependencies:<SelectPackageManager/></Step>

<Bash>
<PackageManager/> @radix-ui/react-switch
</Bash>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource  name={["ui/react/switch"]} extension={["tsx"]} />
</Steps>
</TabsContent>
</Tabs>

## Options

### Variant

Use the `variant` prop to customize the switch style.

<ComponentPreview name="demos/ui/react/switch/variant" 
preview={`<div className="flex items-center space-x-2"> 
  <Switch variant="neutral" id="neutral-switch" /> 
  <Label htmlFor="neutral-switch">Neutral</Label>
</div> 
<div className="flex items-center space-x-2"> 
  <Switch variant="primary" id="primary-switch" /> 
  <Label htmlFor="primary-switch">Primary</Label> 
</div> 
<div className="flex items-center space-x-2"> 
  <Switch variant="secondary" id="secondary-switch" /> 
  <Label htmlFor="secondary-switch">Secondary</Label>
</div>`} />

### Size

Use the `size` prop to adjust the size of the switch.

<ComponentPreview name="demos/ui/react/switch/size" 
preview={`<div className="flex items-center space-x-2"> 
  <Switch size="sm" id="small-switch" /> 
  <Label htmlFor="small-switch">Small</Label>
</div> 
<div className="flex items-center space-x-2"> 
  <Switch size="md" id="medium-switch" /> 
  <Label htmlFor="medium-switch">Medium</Label> 
</div> 
<div className="flex items-center space-x-2"> 
  <Switch size="lg" id="large-switch" /> 
  <Label htmlFor="large-switch">Large</Label> 
</div>`} />

### Shape

Use the `shape` prop to customize the shape of the switch.

<ComponentPreview name="demos/ui/react/switch/shape" 
preview={`<div className="flex items-center space-x-2"> 
  <Switch shape="rectangle" id="rectangle-switch" /> 
  <Label htmlFor="rectangle-switch">Rectangle</Label>
</div>
<div className="flex items-center space-x-2">
  <Switch shape="circle" id="circle-switch" />
  <Label htmlFor="circle-switch">Circle</Label>
</div> 
<div className="flex items-center space-x-2"> 
  <Switch shape="square" id="square-switch" /> 
  <Label htmlFor="square-switch">Square</Label> 
</div>`} />

### Disabled

Use the disabled prop to disable the switch.

<ComponentPreview name="demos/ui/react/switch/disabled" 
preview={`<div className="flex items-center space-x-2"> 
  <Switch disabled id="disabled-switch" /> 
  <Label htmlFor="disabled-switch">Disabled</Label>
</div>`} />