---
title: Popover
description: A popover is a transient view that appears above other content on the screen when a user interacts with a control.
component: true
thumbnail:
  image: /previews/components/core/avatar.jpeg
  video: /previews/components/core/avatar.mp4
---

<ComponentPreview
  name="demos/ui/react/popover/default"
  preview={`<Popover content={<Content />} >
    <Button>Open popover</Button>
</Popover>`}
/>

## Installation

<Steps>
<Step>Install the following dependencies:</Step>

```bash
npm install @radix-ui/react-popover
```
  <Step>Copy and paste the following code into your project.</Step>
  <ComponentSource name={["ui/react/popover"]} extension={["tsx"]} />
  <Step>Update the import paths to match your project setup.</Step>
</Steps>

## Options

### Placement

Use the `placement` prop to control the side from which the popover will appear. By default, it opens from the bottom.

<ComponentPreview name="demos/ui/react/popover/placement" 
preview={`<Popover content={<Content />} placement="top"> 
  <Button variant="outline">Open Popover Top</Button> 
</Popover>`} />

### Offset

Use the `offset` prop to adjust the distance between the popover and its trigger.

<ComponentPreview name="demos/ui/react/popover/offset" 
preview={`<Popover content={<Content />} offset={20}> 
  <Button variant="outline">Open Popover with Offset</Button> 
</Popover>`} />

### Arrow

Use the arrow prop to add an arrow pointing to the trigger element.

<ComponentPreview name="demos/ui/react/popover/arrow" 
preview={`<Popover content={<Content />} arrow> 
  <Button variant="outline">Open Popover with Arrow</Button> 
</Popover>`} />

### Composition

Here’s a full example combining all the components: PopoverTrigger, PopoverContent, and Popover.

<ComponentPreview name="demos/ui/react/popover/composition" 
preview={`<Popover>
  <PopoverTrigger>Open</PopoverTrigger>
  <PopoverContent>Place content for the popover here.</PopoverContent>
</Popover>`} 
/>